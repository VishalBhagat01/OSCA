FRONTEND_ORIGIN=http://localhost:5173
CALLBACK_ALLOWED_HOSTS=localhost,127.0.0.1
AGENT_API_KEY=

# ─── LLM Provider ───────────────────────────────────────────
# Choose: "ollama" (local, default) or "gemini" (cloud API, faster)
LLM_PROVIDER=ollama

# ─── Ollama Settings (used when LLM_PROVIDER=ollama) ────────
OLLAMA_MODEL=qwen2.5-coder:7b
OLLAMA_MAX_TOKENS=4096
OLLAMA_NUM_CTX=8192
OLLAMA_TEMPERATURE=0.2

# ─── Gemini Settings (used when LLM_PROVIDER=gemini) ────────
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
GEMINI_TEMPERATURE=0.2
GEMINI_MAX_TOKENS=4096

# ─── NVIDIA Nemotron Fallback Settings ──────────────────────
# Automatically used when Gemini fails with quota/rate-limit errors
NVIDIA_API_KEY=
NVIDIA_MODEL=nvidia/llama-3.1-nemotron-70b-instruct
NVIDIA_TEMPERATURE=0.2
NVIDIA_MAX_TOKENS=4096
NVIDIA_FALLBACK_ENABLED=true
